Target redesign direction:

- Keep the launcher dark and green, but move away from the older plain tabbed screen.
- Put the `Vanilla` action in the top-right corner as a first-class action.
- Show versions from old to new: `1.8` through `1.21.5`.
- Surface mod compatibility at the row level.
- Distinguish three states for mods:
  enabled compatible,
  enabled incompatible,
  downloaded but not active.
- Animate section reveal and button hover in a restrained Minecraft-launcher-like way.
- Keep the runtime UI grounded in the actual backend API added in this staged set.
